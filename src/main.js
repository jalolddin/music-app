import { createApp } from 'vue'
import App from './App.vue'
import './assets/style.css'
const fs = require('fs');
// const __dirname = require()
// const app = express();
// app.use(express.static(require(__dirname)))
createApp(App).mount('#app')
